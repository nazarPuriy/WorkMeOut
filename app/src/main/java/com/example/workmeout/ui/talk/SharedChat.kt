package com.example.workmeout.ui.talk

import android.os.Bundle
import android.view.View
import android.widget.TextView
import androidx.appcompat.app.AppCompatActivity

import com.example.workmeout.R
import org.w3c.dom.Text

/*
import com.cenkgun.chatbar.ChatBarView*/
class SharedChat : AppCompatActivity() {
/*
    private lateinit var textView: TextView
    private lateinit var chatBarView: ChatBarView*/

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.shared_chat)
/*
        textView = findViewById(R.id.textView_chat)
        chatBarView = findViewById(R.id.chatBar)

        //Setup
        chatBarView.setMessageBoxHint("Name")
        chatBarView.setSendClickListener(View.onClickListener() {

        })*/
    }
}